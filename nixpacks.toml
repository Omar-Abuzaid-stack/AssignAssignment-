[phases.setup]
nixPkgs = ["python310", "nodejs", "chromium"]

[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "playwright install chromium",
  "playwright install-deps",
  "npm install -g markdown-pdf"
]
